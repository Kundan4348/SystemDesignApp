// Additional scenario-based quizzes — 15 more per week

const QUIZZES_EXTRA = [
    {
        id: "week1-scenarios",
        title: "Week 1: Scenario Challenges",
        subtitle: "Real-world decision making",
        questionCount: 15,
        questions: [
            { q: "Your startup has 1,000 users. CTO wants microservices + Kubernetes + Redis cluster + Kafka. What do you advise?", options: ["Great idea, build for scale early", "Overkill — use a single server + PostgreSQL, scale when needed", "Use serverless instead", "Start with microservices but skip Kubernetes"], correct: 1, explanation: "At 1,000 users, a single server handles everything. Premature architecture adds development time, operational complexity, and cost without benefit. Scale when you actually need to." },
            { q: "Your app stores session data in a cookie (JWT). Is this stateless or stateful?", options: ["Stateful — session data exists somewhere", "Stateless — server doesn't store anything, session is in the token itself", "Neither — it's token-based", "Depends on the JWT size"], correct: 1, explanation: "JWT is stateless from the server's perspective — all session data is encoded in the token itself. Server just validates the signature. No server-side storage needed. This is why JWTs enable horizontal scaling trivially." },
            { q: "Your load balancer uses health checks every 5 seconds. A server starts returning errors but doesn't crash. Users get errors for up to 5 seconds. How to fix?", options: ["Check health every 100ms", "Use circuit breaker pattern alongside LB health checks", "Add more servers", "Increase timeout"], correct: 1, explanation: "Health checks have granularity limits. Circuit breaker at the application level detects errors immediately (after N failures) and stops routing to that server. Works faster than LB health check interval." },
            { q: "You cache user profiles (TTL: 1 hour). User updates their name. For the next hour, they see their old name. What pattern fixes this?", options: ["Set TTL to 1 second", "Read-your-own-writes: after user updates, invalidate THEIR cache immediately", "Don't cache user profiles", "Use write-through cache"], correct: 1, explanation: "Read-your-own-writes: when a user modifies their own data, invalidate/update their specific cache entry immediately. Other users can see stale data briefly (acceptable), but the author always sees their latest change." },
            { q: "You have a PostgreSQL database with 500M rows. Full table scans take 30 seconds. Adding an index on 'status' column (3 possible values: active/inactive/banned) doesn't help. Why?", options: ["Index is corrupted", "Low cardinality — index on 3 values isn't selective enough. DB still scans most rows.", "PostgreSQL doesn't support index on enums", "Need to VACUUM first"], correct: 1, explanation: "Low cardinality index! If 80% of rows are 'active', the index doesn't help — DB still reads 80% of the table. Indexes work best on high-cardinality columns (email, user_id) where each value matches very few rows." },
            { q: "Your NoSQL database (DynamoDB) needs a new query: 'Find all orders with status=PENDING sorted by date.' Your partition key is order_id. What do you do?", options: ["Scan the entire table and filter", "Create a Global Secondary Index (GSI) with PK=status, SK=date", "Change the partition key to status", "Use SQL instead"], correct: 1, explanation: "GSI lets you query by different attributes without changing the base table design. GSI with PK=status, SK=date serves this exact access pattern efficiently. This is why DynamoDB supports up to 20 GSIs per table." },
            { q: "CDN cache hit ratio is 40% (should be 90%). Static assets have unique filenames (app.abc123.js) but dynamic API responses are also being routed through CDN. What's wrong?", options: ["CDN is too small", "Dynamic API responses are uncacheable (unique per user) — they're polluting the cache and evicting static assets", "DNS is misconfigured", "TTL is too long"], correct: 1, explanation: "Sending uncacheable dynamic responses through CDN wastes edge storage and causes evictions of actual cacheable content. Route only static assets through CDN. API calls should bypass CDN and go directly to your servers." },
            { q: "You're using DNS round-robin for load balancing. One of your 3 servers crashes. 33% of users get errors. DNS TTL is 5 minutes. How long before all users recover?", options: ["Immediately", "5-15 minutes (DNS caches must expire before clients stop hitting dead server)", "24 hours", "Never — DNS doesn't do health checks"], correct: 1, explanation: "DNS-based LB has NO health checks. Dead server stays in DNS rotation until you manually remove it, AND existing DNS caches must expire (TTL). That's why dedicated load balancers with health checks are preferred for application traffic." },
            { q: "Your write-back cache (write to cache, async flush to DB) loses data when the cache server crashes. 50 writes lost. How to prevent this?", options: ["Don't use write-back cache", "Use Redis with AOF persistence (fsync every second) — max 1 second of data loss", "Write to two cache servers simultaneously", "Accept the risk — it's the trade-off of write-back"], correct: 1, explanation: "Redis AOF (Append Only File) with fsync=everysec loses at most 1 second of writes on crash. Combined with replication (data on replica too), you get write-back speed with minimal data loss risk. Still not zero-loss though — for zero-loss, use write-through." },
            { q: "Your database has leader in US-East, replica in EU-West. EU users experience 200ms read latency because reads go to US leader. Fix?", options: ["Move all users to US", "Route EU users' reads to the EU replica (read from local replica)", "Add cache in EU", "Increase network bandwidth"], correct: 1, explanation: "Read from local replica! Route EU reads to EU-West replica (20ms) instead of US-East leader (200ms). Writes still go to US leader. Trade-off: EU reads might be slightly stale (replication lag), but for most apps that's acceptable." },
            { q: "Your Redis cache uses 90GB of memory. 80% of cached data is accessed less than once per day. You're paying $500/month for this large instance. How to optimize?", options: ["Upgrade to 128GB", "Set shorter TTL to evict cold data — keep only hot 20% in a smaller (cheaper) instance", "Move to Memcached", "Shard across more nodes"], correct: 1, explanation: "The 80/20 rule: 20% of data serves 80% of requests. Set TTL so cold data expires. A 20GB instance (at $100/month) with proper TTL would have nearly the same hit ratio because hot data stays cached. Don't cache the long tail." },
            { q: "Your API returns user data. Mobile app caches responses. You push a critical security update that changes the response format. Old mobile apps crash. How to prevent this in the future?", options: ["Force users to update", "API versioning — old apps hit /v1 (unchanged), new apps hit /v2 (new format)", "Never change response format", "Use GraphQL"], correct: 1, explanation: "API versioning! Old clients continue using /v1 (backwards compatible). New clients use /v2. You maintain both for a deprecation period. This is why \"version your APIs from day one\" is a golden rule." },
            { q: "You sharded your database by user_id. A new feature needs: 'Get the 10 most recent posts across ALL users.' This query now requires hitting all shards. What's the standard fix?", options: ["Remove sharding", "Maintain a separate 'recent_posts' table/service that aggregates from all shards (fan-in)", "Use a search index (Elasticsearch) that indexes all posts", "Only show posts from the user's own shard"], correct: 2, explanation: "Cross-shard aggregation queries should use a dedicated search/analytics service. Elasticsearch indexes data from all shards, enabling global queries without scatter-gather. The search index is eventually consistent but that's fine for a feed." },
            { q: "Your system uses eventual consistency. A user updates their email, immediately opens 'Account Settings', and sees their OLD email. They think the update failed and submit again. How to fix the UX?", options: ["Switch to strong consistency everywhere", "Optimistic UI: show the new value immediately on client-side, regardless of backend state", "Add a loading spinner for 5 seconds", "Tell users to refresh"], correct: 1, explanation: "Optimistic UI: after the user submits, immediately update the UI with the new value (the response from the write confirms it). Don't re-fetch from a potentially-stale read replica. The client already knows the truth — it just wrote it." },
            { q: "Interview question: 'Design a URL shortener.' You immediately start drawing a database schema. The interviewer looks disappointed. What should you have done first?", options: ["Ask about the tech stack", "Ask clarifying questions about requirements: scale, features, constraints", "Estimate QPS first", "Discuss competing products"], correct: 1, explanation: "ALWAYS clarify requirements first! 'How many URLs/day? Do they expire? Custom aliases? Analytics needed? What's the availability target?' Jumping to solution without understanding the problem is the #1 mistake in system design interviews." }
        ]
    },
    {
        id: "week2-scenarios",
        title: "Week 2: Architecture Decisions",
        subtitle: "Tradeoff reasoning",
        questionCount: 15,
        questions: [
            { q: "You're designing an e-commerce checkout. Payment service is async (Kafka). User clicks 'Pay' but gets no confirmation for 3 seconds. UX team is unhappy. Solution?", options: ["Make payment synchronous", "Optimistic response: show 'Processing...' immediately, confirm via WebSocket/push when payment completes", "Speed up Kafka", "Skip payment confirmation"], correct: 1, explanation: "Async doesn't mean slow UX. Accept the payment request synchronously (instant response: 'Processing'), process via queue, notify user via WebSocket/push when done. User sees instant feedback, backend remains decoupled." },
            { q: "Your Kafka consumer group has 10 consumers but the topic only has 3 partitions. How many consumers are actually processing messages?", options: ["10 — all active", "3 — only one consumer per partition. 7 are idle!", "1 — only the leader", "Depends on message volume"], correct: 1, explanation: "Kafka rule: max consumers in a group = number of partitions. With 3 partitions, only 3 consumers get assigned partitions. The other 7 sit idle (standby). To use all 10, you need at least 10 partitions." },
            { q: "You use fixed window rate limiting (100 req/min). At 12:00:59, a user sends 100 requests. At 12:01:01 (new window), they send 100 more. In 2 seconds, they sent 200 requests. Is this a problem?", options: ["No — each window is valid", "Yes — this is the boundary burst problem. Effective rate is 2x the limit at window edges.", "Only if the server can't handle it", "This can't happen with fixed windows"], correct: 1, explanation: "Fixed window boundary burst: 100 requests at end of window + 100 at start of next = 200 in 2 seconds. Double the intended rate! Fix: sliding window counter (weighted overlap) or token bucket (no windows at all)." },
            { q: "API endpoint POST /orders creates orders. Network glitch causes client to retry. Now there are 2 identical orders. The API is not idempotent. Best fix?", options: ["Disable retries", "Add Idempotency-Key header — server stores result by key, returns cached result on retry", "Use PUT instead of POST", "Add deduplication at database level"], correct: 1, explanation: "Idempotency key: client generates unique key per logical operation. Server checks: 'Have I processed key X before?' If yes → return stored result. If no → process and store. Eliminates duplicates from retries." },
            { q: "You have 4 cache servers using consistent hashing. Server 3 fails. What happens to its keys?", options: ["All keys are redistributed to all servers", "Server 3's keys move to the next server clockwise on the ring (Server 4 or next)", "Keys are lost permanently", "All requests fail until Server 3 recovers"], correct: 1, explanation: "Consistent hashing: each key moves to next clockwise server. Only Server 3's keys (~25%) are affected — they map to the next server on the ring. The other 75% of keys are completely unaffected. Minimal disruption." },
            { q: "Your database is sharded by order_id. You need to find 'all orders for user X'. This requires querying all shards. What would have been a better shard key?", options: ["timestamp", "user_id (so all user's orders are on same shard)", "product_id", "status"], correct: 1, explanation: "Shard key should match your primary access pattern. If most queries are 'orders for user X', shard by user_id — all that user's orders on one shard, single-shard query. The lesson: choose shard key based on HOW you query, not what the primary key is." },
            { q: "Quorum system: N=5 nodes, W=3, R=1. Is this strongly consistent?", options: ["Yes — W+R > N (3+1=4 < 5) — wait, NO!", "No — W+R must be > N. 3+1=4 ≤ 5. No overlap guaranteed.", "Yes — writing to majority is enough", "Depends on the data"], correct: 1, explanation: "W+R must be > N for strong consistency. 3+1=4 is NOT > 5. There's no guaranteed overlap between write and read quorums. A read might hit 1 node that didn't receive the latest write. For N=5: W=3, R=3 (6>5) works." },
            { q: "Your system uses AP (availability over consistency). During a network partition, both sides accept writes. Partition heals. Now you have conflicting writes. How to resolve?", options: ["Pick the latest write (Last Write Wins)", "Merge both writes if possible (CRDTs)", "Ask the user to resolve", "All of these are valid strategies depending on the use case"], correct: 3, explanation: "All are valid! LWW: simple but lossy (Cassandra default). CRDTs: complex but no data loss (counters, sets). User resolution: for complex conflicts (Google Docs). The choice depends on data semantics. In interview, mention all three and say which you'd pick for your case." },
            { q: "You implement a token bucket rate limiter in Redis. INCR + EXPIRE are two separate commands. Between them, the server crashes. What happens?", options: ["Nothing — Redis is atomic", "Counter increments but never expires — grows forever, eventually blocks everyone", "Counter resets to 0", "Redis auto-recovers"], correct: 1, explanation: "Race condition! INCR succeeds (counter=1), crash before EXPIRE. Key exists but has no TTL. Counter grows forever, never resets. Fix: use a Lua script to make INCR + EXPIRE atomic, or use Redis SET with EX flag, or MULTI/EXEC transaction." },
            { q: "Kafka vs SQS: your system needs exactly-once processing (each message processed once and only once). Which is easier to achieve this with?", options: ["SQS — built-in exactly-once", "Kafka — has exactly-once semantics built in (idempotent producer + transactional consumer)", "Neither — true exactly-once is impossible", "Both — equally easy"], correct: 1, explanation: "Kafka supports exactly-once semantics (EOS) since version 0.11: idempotent producer (dedup on broker) + transactional consumer (atomic read-process-write). SQS only offers at-least-once. True exactly-once across distributed systems is hard but Kafka gets very close within its own ecosystem." },
            { q: "You design a REST API. GET /users returns 10 million users. The response is 2GB. What's wrong and how to fix?", options: ["Use gzip compression", "ALWAYS paginate collection endpoints. GET /users?limit=20&cursor=abc", "Return only IDs and fetch details separately", "Use streaming response"], correct: 1, explanation: "Never return unbounded collections! Always paginate with limit + cursor. Default limit=20, max limit=100. This protects both server (memory) and client (download time). Cursor-based pagination for consistent results even when data changes." },
            { q: "Your API has a breaking change (removing a field). 1M mobile users have cached the old app. You deploy the new API. What happens?", options: ["Nothing — apps auto-update", "1M users experience crashes or broken features until they update their app", "Mobile apps gracefully handle missing fields", "CDN serves old response format"], correct: 1, explanation: "Breaking change without versioning = breaking 1M users. Mobile apps don't auto-update. Always: (1) version your API (/v1, /v2), (2) keep old version running during deprecation period, (3) add new fields (non-breaking), never remove old ones without new version." },
            { q: "Consistent hashing with 3 physical nodes and 0 virtual nodes. Node A owns 60% of the ring, B owns 30%, C owns 10%. How to fix?", options: ["Add more physical nodes", "Add 100+ virtual nodes per physical node — distributes ownership evenly across the ring", "Manually assign key ranges", "Use mod hashing instead"], correct: 1, explanation: "Virtual nodes fix uneven distribution. 3 servers × 150 virtual nodes = 450 evenly-distributed points on the ring. Each physical server owns ~33% regardless of how the hash function distributes their initial positions." },
            { q: "Interview: you're designing a chat system. Interviewer asks 'How do you handle message ordering?' You say 'timestamps.' They push back: 'What if two messages have the same timestamp?'", options: ["Use higher precision (nanoseconds)", "Use Lamport clock or vector clock for causal ordering, with server-assigned sequence numbers as tiebreaker", "It's unlikely enough to ignore", "Use UUID as message ID"], correct: 1, explanation: "Timestamps alone don't guarantee ordering (clock drift between servers, same-millisecond messages). Use sequence numbers per conversation (monotonically increasing, assigned by server). For distributed systems, Lamport/vector clocks establish causal ordering. This shows distributed systems depth." },
            { q: "Your rate limiter is centralized in one Redis instance. That Redis goes down. All rate limiting stops. Attackers flood your system. What's the defense-in-depth approach?", options: ["Add Redis replica", "Multi-layer: (1) local in-memory rate limit per server (rough), (2) Redis for global limit, (3) WAF/CDN rate limit as last defense", "Switch to database for rate limiting", "Accept the risk"], correct: 1, explanation: "Defense in depth: multiple layers. Local rate limit (per-server, memory) catches obvious abuse even if Redis is down. Redis (global, precise) provides coordinated limits. CDN/WAF (edge) stops DDoS before it reaches your infrastructure. No single point of failure." }
        ]
    },
    {
        id: "week3-scenarios",
        title: "Week 3: System Estimation & Design",
        subtitle: "Numbers and architecture",
        questionCount: 10,
        questions: [
            { q: "You're estimating storage for Instagram. 100M photos/day, average 2MB each. How much storage per year?", options: ["73 PB/year (100M × 2MB × 365)", "7.3 PB/year", "730 TB/year", "73 TB/year"], correct: 0, explanation: "100M × 2MB = 200TB/day. 200TB × 365 = 73PB/year. This is why Instagram/Facebook build their own data centers — commercial cloud storage at this scale would cost billions. They also use multiple quality levels and tiered storage." },
            { q: "Back-of-envelope: A single server can handle 10,000 QPS. Your system needs 50,000 QPS at peak. Minimum servers needed?", options: ["5 servers", "5 servers + 2-3 extra for redundancy = 7-8 servers", "50 servers", "1 powerful server"], correct: 1, explanation: "50K/10K = 5 servers minimum. But NEVER run at 100% capacity — you need headroom for failures and spikes. Standard: run at 60-70% capacity. So 5/0.7 ≈ 7 servers. Plus 1-2 for redundancy. Plan for N+2 (can lose 2 servers and still serve peak)." },
            { q: "You're designing Elasticsearch for 1TB of product data. Each document averages 1KB. How many documents is that?", options: ["1 million", "1 billion", "100 million", "10 billion"], correct: 1, explanation: "1TB = 1,000,000,000 KB. At 1KB per doc: 1 billion documents. At this scale, you'd need ~5 shards (200GB each) with 1 replica each = 10 total shard copies across 5+ nodes." },
            { q: "WebSocket connections: your chat app has 10M concurrent users. Each WebSocket uses ~10KB of server memory. How much RAM just for connections?", options: ["100 MB", "1 GB", "10 GB", "100 GB"], correct: 3, explanation: "10M × 10KB = 100GB just for connection state! This is why chat systems need many servers (each holding ~100K-500K connections). At 500K per server: 10M/500K = 20 chat servers minimum." },
            { q: "Snowflake ID generator: you have 1024 machine IDs (10 bits). Each machine generates 4096 IDs/ms (12 bits). Your service has 50 machines. What's the max IDs/second across all machines?", options: ["204,800/sec", "204,800,000/sec (50 × 4096 × 1000)", "4,096,000/sec", "50,000/sec"], correct: 1, explanation: "Each machine: 4096 IDs/ms × 1000ms = 4,096,000 IDs/sec. 50 machines: 50 × 4,096,000 = 204,800,000 IDs/sec (~200M/sec). More than enough for any system. Snowflake scales linearly with machines." },
            { q: "Your news feed uses fan-out on write. A celebrity with 50M followers posts. How many write operations does this trigger?", options: ["1 write", "50 writes (one per shard)", "50 million writes (one per follower's feed cache)", "5,000 writes"], correct: 2, explanation: "Fan-out on write: push to EVERY follower's feed cache. 50M followers = 50M cache writes. At 100K writes/sec (Redis), that's 500 seconds (8+ minutes!) to fan out one post. This is exactly why the hybrid approach uses fan-out on READ for celebrities." },
            { q: "You need sub-100ms autocomplete response globally. Your trie is in US-East. Users in Tokyo experience 150ms latency (network alone). How to fix?", options: ["Optimize the trie", "Replicate trie to multiple regions — Tokyo users hit local trie replica", "Use CDN for autocomplete responses", "Accept 150ms"], correct: 1, explanation: "If network latency alone exceeds your budget, you MUST put compute closer to users. Replicate the trie service + data to Tokyo region. CDN could also work for common prefixes (cache popular query results at edge)." },
            { q: "Adaptive bitrate streaming: a 2-hour 4K movie in ALL quality levels (240p through 4K) × 3 codecs = how many total files to store?", options: ["1 file", "6 files (6 resolutions)", "~4,320 files (6 res × 3 codecs × 720 ten-sec segments)", "18 files"], correct: 2, explanation: "2 hours = 7200 seconds / 10-sec segments = 720 segments. Each segment × 6 resolutions × 3 codecs = 12,960 files (even more than my estimate!). This is why video platforms need petabytes of storage. Each \"video\" is actually thousands of segment files." },
            { q: "Your web crawler needs to crawl 1 billion pages. Each DNS lookup takes 50ms. Without DNS caching, how long would DNS resolution alone take?", options: ["50 seconds", "14 hours", "578 days", "50 million seconds (~1.5 years!)"], correct: 2, explanation: "1B × 50ms = 50,000,000 seconds = 578 days! This is why DNS caching is critical in crawlers. With caching (same domain = cached result): most lookups are 0ms. Only ~10M unique domains need actual DNS resolution." },
            { q: "Google SRE says: 'error budget.' Your SLA is 99.9% uptime (43 min downtime/month allowed). You've used 30 minutes this month. A risky deploy is planned. What do you do?", options: ["Deploy anyway", "You have 13 minutes of budget left — deploy during low-traffic with instant rollback ready, or wait until next month", "Cancel the deploy entirely", "SLAs don't matter"], correct: 1, explanation: "Error budget thinking: you have 13 minutes left. If the deploy risks > 13 minutes of downtime, wait for next month's budget. If it's low-risk with instant rollback capability (< 2 min to detect + rollback), proceed cautiously. This quantifies risk instead of guessing." }
        ]
    }
];

// Merge extra quizzes into QUIZZES array
QUIZZES.push(...QUIZZES_EXTRA);
